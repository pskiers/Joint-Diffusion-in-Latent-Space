model:
  logdir: /net/tscratch/people/plgjoaxkal/Joint-Diffusion-in-Latent-Space/
  model_type: multilabel_classifier
  params:
    classifier_test_mode: "resnet"
    image_size: 256 # 32 for latent
    channels: 1
    monitor: val/auroc
    in_features: 2048 # encoder+resnet18 512, resnet50 2048, linear 4096
    num_classes: 15
    dropout: 0.3
    learning_rate: 0.0003
    weight_decay: 0.0001

    first_stage_config:
      target: ldm.models.autoencoder.AutoencoderKL
      params:
        embed_dim: 4
        monitor: "val/rec_loss"
        ckpt_path: "/home/jk/Joint-Diffusion-in-Latent-Space/logs/compvis32x32x4_all_randomresizedcrop_Autoencoder_2024-01-17T13-15-27/checkpoints/epoch=000025.ckpt"
        ddconfig:
          double_z: True
          z_channels: 4
          resolution: 256
          in_channels: 1
          out_ch: 1
          ch: 128
          ch_mult: [ 1,2,4,4 ]  # num_down = len(ch_mult)-1
          num_res_blocks: 2
          attn_resolutions: []
          dropout: 0.0
        lossconfig:
          target: torch.nn.Identity


dataloaders:
  name: chest_xray_nih
  train_batches: [ 16 ]
  val_batch: 16
  num_workers: 4
  num_labeled: null
  pin_memory: True
  persistent_workers: True


lightning:
  trainer:
    benchmark: True
    checkpoint_callback: True
    max_steps: 1000000
    gpus: -1
    strategy: ddp
    # accumulate_grad_batches: 2